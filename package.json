{
  "name": "vite-website-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -C victorvasilev.dev"
  },
  "devDependencies": {
    "vite": "^4.5.1"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
